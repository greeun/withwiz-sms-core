/**
 * Static values that describe message composition itself, independent of any provider.
 */

/** Maximum byte length of a single SMS: 90 bytes on an EUC-KR basis, per the Aligo specification. */
export const SMS_MAX_BYTES = 90;
